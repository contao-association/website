parameters:
    memberships:
        active:
            price: 80
            custom: false
            group: 1
            type: Aktivmitglied

        passive:
            price: 50
            custom: false
            group: 2
            type: Passivmitglied

        support:
            price: 200
            custom: true
            group: 3
            type: Fördermitglied

services:
    _defaults:
        autoconfigure: true
        autowire: true
        bind:
            $memberships: '%memberships%'
            $projectDir: '%kernel.project_dir%'

    App\:
        resource: ../src
